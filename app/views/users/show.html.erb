<% provide(:title, @user.name) %>

<table class="table table-striped">
	<thead>
		<tr>
			<th> Name </th>
			<th> email </th>
			<th> Faculty </th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<th><%= @user.name %></th>
			<th><%= @user.email %></th>
			<th><%= @user.faculty.name %></th>
		</tr>
	</tbody>

	<table>